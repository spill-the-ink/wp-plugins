# Remote WordPress install used by the Vite dev proxy for `?mode=remote`.
# Point it at any site running the plugin. Leave empty to disable the proxy
# and rely on `?mode=local` fixtures.
PC_REMOTE_URL=

# Set to 1 when the remote uses a self-signed certificate (.local dev domains).
PC_ALLOW_SELF_SIGNED=1
